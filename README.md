# NeuroTech Events frontend

React, TypeScript, and Vite client for the NeuroTech Events platform.

```bash
npm ci
npm run dev
```

Run `npm run check` before committing. See the repository-level README and SECURITY guide for architecture and security requirements.

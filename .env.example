# Browser-safe frontend configuration (Vite exposes only VITE_* variables).
# Base URL of the FastAPI backend. Leave empty to keep checkout in local demo mode.
VITE_API_BASE_URL=http://localhost:8000
